# fly.toml app configuration file generated for mosin-bay on 2025-12-05T11:49:20-05:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'mosin-bay'
primary_region = 'iad'

[build]

# Discord bots don't expose HTTP services, they connect via WebSocket
# Remove [http_service] section and configure as a long-running process

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1

[env]
  # Add any non-secret environment variables here
  PREFIX = "!"
